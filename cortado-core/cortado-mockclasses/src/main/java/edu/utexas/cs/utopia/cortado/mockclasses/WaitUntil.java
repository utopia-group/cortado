package edu.utexas.cs.utopia.cortado.mockclasses;

// TODO: remove this
public class WaitUntil {
    /**
     * A stub so that legacy benchmark implementations still compile
     */
    public static void waituntil(boolean guard) {}
}
